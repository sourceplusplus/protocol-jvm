mutation addDataRedaction($id: String!, $type: RedactionType!, $lookup: String!, $replacement: String!){
    addDataRedaction (id: $id, type: $type, lookup: $lookup, replacement: $replacement){
        id
        type
        lookup
        replacement
    }
}