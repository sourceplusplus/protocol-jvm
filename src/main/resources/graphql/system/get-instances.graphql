query getInstances($serviceId: String!){
    getInstances(serviceId: $serviceId) {
        id
        name
        language
        instanceUUID
        attributes {
            key
            value
        }
    }
}